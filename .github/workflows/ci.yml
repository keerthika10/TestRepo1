
name: CI

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Deploy environment"
        required: false
        default: "staging"

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      actions: read
    env:
      REGISTRY: ${REGISTRY:-registry.example.com}
      IMAGE: ${IMAGE:-my-app}
      TAG: ${TAG:-$CI_COMMIT_SHA}
      NAMESPACE: default

    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build
        run: echo 'Perform build for Dockerized App'
      - name: Lint
        run: echo 'Run linter for Dockerized App'
      - name: Test
        run: echo 'Run tests for Dockerized App'
      - name: Codescan
        run: echo 'Run code scanning for Dockerized App'
      - name: Package
        run: echo 'Package Dockerized App'
      - name: Publish
        run: echo 'Publish artifacts for Dockerized App'
      - name: Deploy
        run: echo 'Deploy Dockerized App (implement your deploy step)'