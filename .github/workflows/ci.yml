name: Hello World 6th mar testing

on: 
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: echo "Hello, Keerthika!"