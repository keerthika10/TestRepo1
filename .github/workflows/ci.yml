name: Hello World Workflow
on:
  workflow_dispatch:
  push:
  workflow_dispatch:
jobs:
  print_hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: echo "Hello World"